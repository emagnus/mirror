<div class="small dimmed">
<table>

{{#each yr}}
  <tr>
      <td>{{temperatur}} &deg; / </td>
      <td>
      	{{#if nedbor.max}}
      		{{nedbor.min}} - {{nedbor.max}} mm
      	{{else}}
      		{{nedbor.value}} mm	
      	{{/if}}</td>
  </tr>
{{/each}}
</table>

</div>